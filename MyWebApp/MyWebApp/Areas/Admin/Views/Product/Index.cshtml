@model MyWebApp.Models.ViewModel.ProductVM

<div class="container p-4">
    <div class="row p-3">
        <div class="col-6">
            <h2 class="text-primary">List Of Products</h2>
        </div>
    </div> 
    <div class="col-6 text-end">
        <a class=" btn btn-primary" asp-action="CreateUpdate" asp-controller="Product"><i class="bi bi-plus-circle-fill"></i>&nbsp; Create New Product</a>
    </div>
   
        <div class="col-6 text-lg-start">
            <a class="btn btn-primary" asp-action="DownloadReport" asp-controller="Product">Download Excel Report </a>
        </div>
   

</div>
<table id="myTable" class=" table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>Name</th>
            <th>Description</th>
            <th>Price</th>
            <th>Category</th>
            <th>Actions</th>
        </tr>
    </thead>
@*  <tbody>
       @foreach (var item in Model.Products)
        {
            <tr>
                <td> @item.Name</td>
                <td> @item.Description</td>
                <td> @item.Price</td>
                <td> @item.Category</td>

                


                <td role="group">
                    <a asp-action="createupdate" asp-controller="category" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i></a>
                    <a asp-action="delete" asp-controller="category" onclick="return confirm('are you sure you want to delete this?')" asp-route-id="@item.Id"><i class="bi bi-trash3-fill"></i></a>
                </td>
            </tr>

        }
    </tbody>*@
</table>

@section scripts{
    <script src="~/js/Product.js"></script>
}
